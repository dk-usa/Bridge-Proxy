---
phase: 03-observability-provider-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/provider-registry.ts
  - tests/unit/services/provider-health.test.ts
autonomous: true
requirements: [OBS-01, OBS-04]
user_setup: []
must_haves:
  truths:
    - 'Health status updates automatically after each request'
    - 'Health calculation uses rolling window of last 100 requests'
    - 'Success rate thresholds determine health status'
  artifacts:
    - path: 'src/services/provider-registry.ts'
      provides: 'Health calculation logic with rolling window'
      contains: 'calculateHealthStatus'
    - path: 'tests/unit/services/provider-health.test.ts'
      provides: 'Health calculation unit tests'
      min_lines: 50
  key_links:
    - from: 'src/admin-store.ts'
      to: 'providerRegistry.recordSuccess/recordError'
      via: 'addLog method'
      pattern: 'recordSuccess|recordError'
---

# Phase 03 Plan 01: Health Calculation with Rolling Window

<objective>
Implement automatic health status calculation based on rolling window of recent request outcomes.

**Purpose:** Enable real-time provider health tracking so admins can identify issues before users report them.

**Output:** Health calculation logic integrated into existing provider registry with comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-observability-provider-health/03-CONTEXT.md
@.planning/phases/03-observability-provider-health/03-RESEARCH.md
@src/services/provider-registry.ts
@src/admin-store.ts
</context>

<interfaces>
<!-- Key types and contracts executor needs. Extracted from codebase. -->

From src/services/provider-registry.ts:

```typescript
export interface ProviderStatus {
  id: string;
  status: 'healthy' | 'degraded' | 'unhealthy';
  latencyMs: number | null;
  lastCheck: string | null;
  successCount: number;
  errorCount: number;
  totalCount: number;
}

// Methods to update:
recordSuccess(id: string): void
recordError(id: string): void
```

From src/admin-store.ts:

```typescript
addLog(log: RequestLog): void {
  if (log.provider) {
    if (log.status === 'success') {
      providerRegistry.recordSuccess(log.provider);
    } else {
      providerRegistry.recordError(log.provider);
    }
  }
}
```

Health calculation thresholds (from CONTEXT.md D-03):

- ≥95% success = healthy
- 80-94% success = degraded
- <80% success = unhealthy
- latency > 5000ms = degraded (D-04)
  </interfaces>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add rolling window fields to ProviderStatus</name>
<files>src/services/provider-registry.ts, tests/unit/services/provider-health.test.ts</files>
<behavior>
- Test 1: ProviderStatus includes recentOutcomes array field
- Test 2: ProviderStatus includes lastLatencyMs field
- Test 3: New providers initialize with empty recentOutcomes array
</behavior>
<read_first>
- src/services/provider-registry.ts — Current ProviderStatus interface and ProviderRegistry class
- .planning/phases/03-observability-provider-health/03-CONTEXT.md — Health thresholds (D-03, D-04)
</read_first>
<action>
1. Create test file: tests/unit/services/provider-health.test.ts
2. Add tests verifying ProviderStatus has recentOutcomes (boolean[]) and lastLatencyMs (number) fields
3. Run tests (should fail - fields don't exist yet)
4. Modify ProviderStatus interface in src/services/provider-registry.ts:
   - Add `recentOutcomes: boolean[]` — rolling window of last 100 request outcomes
   - Add `lastLatencyMs: number` — latency of most recent successful request
5. Update initializeDefaults() to initialize new fields:
   - `recentOutcomes: []`
   - `lastLatencyMs: 0`
6. Update add() method to initialize new fields for dynamically added providers
7. Run tests — should pass
</action>
<verify>
<automated>npm run test:run -- tests/unit/services/provider-health.test.ts</automated>
</verify>
<done>
- ProviderStatus interface has recentOutcomes and lastLatencyMs fields
- New providers initialize with empty recentOutcomes array
- Unit tests pass (3 tests)
</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement health calculation function</name>
<files>src/services/provider-registry.ts, tests/unit/services/provider-health.test.ts</files>
<behavior>
- Test 1: Empty window returns 'unhealthy' status
- Test 2: 95%+ success rate returns 'healthy'
- Test 3: 80-94% success rate returns 'degraded'
- Test 4: <80% success rate returns 'unhealthy'
- Test 5: Latency > 5000ms returns 'degraded' regardless of success rate
- Test 6: Partial window (50 requests) calculates correctly
- Test 7: Single request (100% or 0%) works correctly
</behavior>
<read_first>
- src/services/provider-registry.ts — Existing ProviderRegistry class structure
- .planning/phases/03-observability-provider-health/03-CONTEXT.md — Thresholds D-03, D-04
- .planning/phases/03-observability-provider-health/03-RESEARCH.md — Edge cases
</read_first>
<action>
1. Add tests for calculateHealthStatus function:
   - Empty window → 'unhealthy'
   - 95%+ success → 'healthy'
   - 80-94% success → 'degraded'
   - <80% success → 'unhealthy'
   - Latency > 5000ms → 'degraded' (override)
   - Partial window works
   - Single request works
2. Run tests (should fail - function doesn't exist)
3. Add private method calculateHealthStatus(recentOutcomes: boolean[], lastLatencyMs: number): 'healthy' | 'degraded' | 'unhealthy'
4. Implementation:
   - If recentOutcomes.length === 0, return 'unhealthy'
   - Calculate successRate = recentOutcomes.filter(Boolean).length / recentOutcomes.length
   - If lastLatencyMs > 5000, return 'degraded' (D-04)
   - If successRate >= 0.95, return 'healthy'
   - If successRate >= 0.80, return 'degraded'
   - Return 'unhealthy'
5. Run tests — should pass (7 tests)
</action>
<verify>
<automated>npm run test:run -- tests/unit/services/provider-health.test.ts</automated>
</verify>
<done>
- calculateHealthStatus function implemented
- All threshold tests pass
- Edge cases handled (empty, partial, single, latency override)
</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Integrate health calculation into recordSuccess/recordError</name>
<files>src/services/provider-registry.ts, tests/unit/services/provider-health.test.ts</files>
<behavior>
- Test 1: recordSuccess adds true to recentOutcomes and recalculates status
- Test 2: recordError adds false to recentOutcomes and recalculates status
- Test 3: Rolling window maintains max 100 outcomes
- Test 4: Consecutive errors transition to unhealthy status
- Test 5: Recovery from unhealthy to healthy works
</behavior>
<read_first>
- src/services/provider-registry.ts — Current recordSuccess and recordError methods
- .planning/phases/03-observability-provider-health/03-CONTEXT.md — D-01, D-02 (per-request updates)
</read_first>
<action>
1. Add tests for recordSuccess/recordError integration:
   - recordSuccess pushes true, updates status
   - recordError pushes false, updates status
   - Window caps at 100 outcomes (oldest removed)
   - Consecutive errors cause unhealthy transition
   - Recovery path works
2. Run tests (should fail - methods don't update health yet)
3. Modify recordSuccess to:
   - Push true to recentOutcomes array
   - Trim to last 100 if exceeded: `status.recentOutcomes = status.recentOutcomes.slice(-100)`
   - Call calculateHealthStatus and update status field
   - Update lastCheck timestamp
4. Modify recordError to:
   - Push false to recentOutcomes array
   - Trim to last 100 if exceeded
   - Call calculateHealthStatus and update status field
   - Update lastCheck timestamp
5. Note: latencyMs is updated by caller (via testProvider endpoint), not here
6. Run tests — should pass (5 tests)
</action>
<verify>
<automated>npm run test:run -- tests/unit/services/provider-health.test.ts</automated>
</verify>
<done>
- Health status auto-updates after each request
- Rolling window maintains last 100 outcomes
- Status transitions correctly based on success rate
</done>
</task>

<task type="auto" tdd="true">
<name>Task 4: Add latency tracking to health calculation</name>
<files>src/services/provider-registry.ts, tests/unit/services/provider-health.test.ts</files>
<behavior>
- Test 1: recordSuccessWithLatency stores latency value
- Test 2: High latency (>5000ms) overrides success-based status to 'degraded'
- Test 3: Normal latency (<5000ms) doesn't override status
</behavior>
<read_first>
- src/services/provider-registry.ts — ProviderRegistry class
- .planning/phases/03-observability-provider-health/03-CONTEXT.md — D-04 (latency threshold)
</read_first>
<action>
1. Add tests for latency-based health degradation:
   - High latency overrides healthy to degraded
   - Normal latency preserves healthy status
   - Latency only tracked on successful requests
2. Run tests (should fail - no latency tracking)
3. Add new method recordSuccessWithLatency(id: string, latencyMs: number):
   - Call recordSuccess(id) to update outcomes
   - Update lastLatencyMs field
   - Recalculate health (latency threshold check happens in calculateHealthStatus)
4. Note: The calculateHealthStatus already checks lastLatencyMs > 5000
5. Run tests — should pass (3 tests)
6. Verify admin-store.ts doesn't need changes — it only calls recordSuccess/recordError
   - Latency is tracked via test endpoint, not regular requests
</action>
<verify>
<automated>npm run test:run -- tests/unit/services/provider-health.test.ts</automated>
</verify>
<done>
- Latency threshold (>5000ms) triggers degraded status
- recordSuccessWithLatency method available for explicit latency tracking
- All 18+ unit tests pass
</done>
</task>
</tasks>

<verification>
After all tasks complete:
1. Run full test suite: `npm run test:run`
2. Verify no regressions in existing tests
3. Check that health calculation tests cover all edge cases
</verification>

<success_criteria>

- [ ] ProviderStatus has recentOutcomes and lastLatencyMs fields
- [ ] calculateHealthStatus function implements threshold logic
- [ ] Health auto-updates after recordSuccess/recordError calls
- [ ] Rolling window maintains last 100 outcomes
- [ ] Latency > 5000ms triggers degraded status
- [ ] All unit tests pass (18+ tests)
      </success_criteria>

<output>
After completion, create `.planning/phases/03-observability-provider-health/03-01-SUMMARY.md`
</output>
